{% extends "layout.html.twig" %}

{% block content %}
    {% for group in groups %}
        <span class="group"><a href="{{ path('group_get', {'id': group.getId()}) }}">{{ group.getName() }}</a></span><br>
    {% endfor %}
{% endblock content %}